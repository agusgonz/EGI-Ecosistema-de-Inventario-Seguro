apiVersion: v1
kind: Service
metadata:
  name: inventario-db-service
spec:
  selector:
    app: inventario-db
  ports:
    - protocol: TCP
      port: 27017     
      targetPort: 27017
  type: ClusterIP      # Tipo interno